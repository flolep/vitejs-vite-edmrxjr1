# Configuration Firebase
# Copiez ce fichier en .env et remplacez les placeholders par vos vraies valeurs
# Les mêmes valeurs doivent être ajoutées dans Netlify UI (Site settings > Environment variables)

VITE_FIREBASE_API_KEY=your-firebase-api-key-here
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_DATABASE_URL=https://your-project.firebaseio.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
VITE_FIREBASE_APP_ID=your-app-id

# Configuration Spotify (côté serveur uniquement, pas de préfixe VITE_)
# Obtenez ces credentials depuis : https://developer.spotify.com/dashboard
# 1. Créez une application Spotify
# 2. Copiez le Client ID et Client Secret
# 3. Ajoutez les Redirect URIs :
#    - https://votresite.netlify.app/callback
#    - https://develop--votresite.netlify.app/callback
#    - https://staging--votresite.netlify.app/callback
#    - http://localhost:5173/callback (pour développement local)
SPOTIFY_CLIENT_ID=your-spotify-client-id-here
SPOTIFY_CLIENT_SECRET=your-spotify-client-secret-here

# Token de sécurité pour n8n (côté serveur uniquement, pas de préfixe VITE_)
# Générez un token aléatoire : openssl rand -base64 32 | head -c 32
# Ajoutez ce token dans Netlify UI ET dans votre workflow n8n
# Voir documentation : docs/n8n-security-setup.md
N8N_AUTH_TOKEN=your-random-token-here
